sudo: required

language: bash

services:
  - docker

script:
- docker build -t canzheng/clearsilver .
- docker run -v test:/ --rm canzheng/clearsilver python2 hello.py

after_script:
- docker images
